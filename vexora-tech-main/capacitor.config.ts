import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.vexor.tech',
  appName: 'Vexor IT Solutions',
  webDir: 'dist/client'
};

export default config;
